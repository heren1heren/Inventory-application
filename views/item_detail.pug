extends layout


block content
 h1= title 
     | : #{item.name} 
 hr
 h4 Description:
    p #{item.description}
 h4 Category:
    p #{item.category.name}
 h4 Price:
    p #{item.price}
 h4 Count:
    p #{item.number}  
 a(href= item.url + '/delete')  Delele Item
 
 div
  a(href= item.url + '/update')   Update Item

