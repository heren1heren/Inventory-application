extends layout


block content 
  h1=title
  hr
  h4  Item Name: #{item.name} 
  h4  Item Description: #{item.description}
  h4 Item Price: #{item.price}
  h4 Item Number: #{item.number}
  p Do you want to delete this item?
  form(method="POST")
    
   button.btn.btn-primary(type='submit') Confirm
   // click button will trigger Post request
